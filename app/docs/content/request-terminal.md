<!-- Monitor all incoming HTTP requests to your mock server in real time from the built-in request terminal.

## What it shows

| Column | Description |
|--------|-------------|
| Timestamp | Time the request was received |
| Method | HTTP method (GET, POST, etc.) |
| Path | Request URL path |
| Status | Response status code returned |
| Duration | Response time in milliseconds |

Click any entry to expand it and inspect the full request headers, query parameters, and body useful for debugging integration issues. -->
